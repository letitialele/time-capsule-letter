# 📄 產品需求文件 (PRD)：「未來的信」時空膠囊（前端簡化版）

## 1. 產品概述
* **產品名稱**：時空膠囊 — 給未來的一封信 (Time Capsule Mail)
* **核心概念**：一個極簡、溫馨、具儀式感的單頁網頁 (SPA)。使用者可以寫下一段話給一個月後或一年後的自己，點擊封存後，系統會自動將文字內容編碼，並生成一張精美的電子信封圖片與專屬時空序號，供使用者截圖或下載留存。
* **目標族群**：喜歡自我對話、熱愛生活儀式感、想記錄當下心境的使用者。

---

## 2. 核心功能需求 (Functional Requirements)

### 2.1 核心使用者流程 (User Flow)
1. **撰寫階段**：使用者進入網頁 ➔ 選擇時間軸（一個月/一年） ➔ 填寫收件人與信件內容 ➔ 點擊「封存」。
2. **儀式動畫**：點擊封存後，表單淡出，播放 1~2 秒的「信件折疊入封」或「膠囊封裝」的微動畫。
3. **生成結果**：展示最終的電子信封與序號，提示使用者長按圖片儲存或截圖留存。

### 2.2 功能清單 (Feature List)
| 功能模組 | 功能描述 | 優先級 (P0-P2) | 備註 |
| :--- | :--- | :--- | :--- |
| **信件撰寫** | 收件人名稱輸入框（To: ______） | P0 | 限 20 字內 |
| **時間選擇** | 切換「一個月後」或「一年後」 | P0 | 影響解封日期的計算 |
| **內容輸入** | 信件主體文字區域 (Textarea) | P0 | 限制 100~500 字以維持卡片美感 |
| **封存觸發** | 點擊「封存這段時光」按鈕 | P0 | 觸發動畫與圖片生成邏輯 |
| **動畫演繹** | 封存時的轉場微動畫 | P1 | 提升儀式感 |
| **信封圖片生成**| 利用前端技術將結果渲染成精美圖卡 | P0 | 包含封存日、解封日與序號 |
| **時空序號生成**| 將內容與日期進行 Base64 加密生成序號 | P1 | 可作為未來「無後端解密頁」的伏筆 |

---

## 3. 頁面結構與 UI 設計規範 (UI/UX Design)

### 3.1 視覺風格
* **風格定位**：極簡、療癒、優雅的法式或日系清新感。
* **色彩計畫**：
  * 背景色：溫暖的米白色（如 `#FAF9F6`）或淡燕麥色。
  * 主文字/線條：深炭灰（如 `#2C3E50`）或暖茶色，避免純黑的生硬感。
* **字體**：偏好具備人文氣息的襯線體（Serif）或乾淨的手寫感字體。

### 3.2 佈局區塊 (Layout)
* **Header**：主標題「時空膠囊 (Time Capsule)」，副標題「寫一封信，寄給未來某個時刻的自己。」
* **Form Area**：簡潔的輸入框，拿掉沉重的邊框，改用底部單線條（Underline）設計。
* **Action Area**：置中的線條感按鈕「封存這段時光 ➔」。

---

## 4. 技術實現方案 (Technical Specifications)

因為是純前端簡化版，不涉及資料庫儲存，技術核心在於**記憶與本地渲染**。

### 4.1 電子信封圖片生成 (Image Generation)
* **技術選型**：使用 `html2canvas` 庫，或是純 HTML5 `<canvas>` API。
* **渲染元件 (DOM)**：當點擊封存時，在幕後或畫面上渲染一個 `#capsule-card` 元件，其包含：
  * 優雅的紙張紋理背景與簡約邊框。
  * 頂部時光郵戳或極簡插畫（如沙漏、海浪線條）。
  * 欄位資訊：`To: [使用者輸入]`, `封存日: [YYYY.MM.DD]`, `解封日: [YYYY.MM.DD + 選擇時間]`。
  * 底部置中顯示「專屬時空序號」。

### 4.2 時空序號生成邏輯
* **生成機制**：為了讓序號具備「真實解密感」，可將 `解封日期 + 收件人 + 部分信件內容` 進行 Base64 編碼，再截取前後綴。
* **序號範例**：`TC-20260530-X89B`
* **彩蛋擴充性**：此序號在未來若開發「解密頁」，前端可直接透過 Base64 解碼還原信件文字，實現**零後端的解密功能**。

---

## 5. 使用者體驗與微互動 (UX & Micro-interactions)

* **輸入反饋**：Input Focus 時，Placeholder 柔和淡出，底部線條變色或加粗。
* **封存轉場**：
  1. 點擊按鈕，按鈕文字變更為「正在封裝時光...」，並加入 Fade-out 動畫。
  2. 撰寫表單向上收合（模擬信件折疊）。
  3. 電子信封卡片帶有微微的 Shadow 浮動與 Fade-in 效果優雅登場。
* **溫馨提示語**：在最終圖片下方顯示：
  > 💡 **時光守護提示**：本專案為單機極簡版，小膠囊不會儲存在網路伺服器上。請**長按儲存圖片**或**截圖留存**。在未來的某一天打開它，看看自己走過了多遠的路。

---

## 6. 非功能性需求 (Non-Functional Requirements)

* **行動端優先 (Mobile First)**：多數使用者會透過手機瀏覽、截圖或長按儲存圖片，RWD 佈局必須完美適應各種手機螢幕比例。
* **輕量化效能**：不依賴大型框架（如 React/Vue），建議使用原生 Vanilla JS + CSS 撰寫，確保網頁加載速度極快。
* **瀏覽器相容性**：確保 `html2canvas` 在 iOS Safari 與 Android Chrome 上皆能正常觸發圖片長按儲存或下載功能。